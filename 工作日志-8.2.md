# 工作日志 8.2 — 杰西卡数据导入问题完整记录

## 时间线：2026-08-01 23:50 ~ 2026-08-02 03:06

## 一、问题发现

### 1.1 初始现象

赵在手机上使用杰西卡时，点击"从云端恢复"后，页面显示"✅ 恢复成功，3 秒后刷新..."，刷新完成后所有数据为空——聊天记录、日记、设置、头像全部丢失。

控制台显示：
```
[CloudSync] 备份版本：v1，当前版本：v4
[CloudSync] 补全了 23 个缺失字段
[CloudSync] 正在从 v1 备份恢复（已补全 23 个字段）...
[CloudSync] 已恢复 33 条数据
✅ 恢复成功，3 秒后刷新...
```

**关键数据：**
- 备份版本：v1（应为 v4）
- 恢复数据量：33 条（应为 1426 条记忆 + 1423 条聊天记录 + 65 篇日记）
- 补全字段：23 个（说明备份格式与当前代码不匹配）

### 1.2 GitHub 备份验证

通过 GitHub API 检查 `urlien/jessica-data` 仓库的 `jessica-backup.json`：

```
_version: 4
_exportedAt: 2026-07-30T18:27:06.309Z
_idb_memories: 1426 条
_idb_messages: 1423 条
_idb_jp_entries: 65 条
jessica_apiBase: ✅
jessica_apiKey: ✅
jessica_messages: 500 条
```

**GitHub 上的备份是 v4 格式，数据完整。** 但杰西卡恢复时下载到的是 v1 格式。

## 二、根因分析

### 2.1 第一个根因：浏览器缓存

**问题：** 浏览器缓存了 GitHub API 的响应。用户之前测试时下载的是 v1 格式的旧备份，浏览器将其缓存。之后每次点"从云端恢复"，浏览器都返回缓存的旧响应（v1），而不是从 GitHub 下载最新的 v4 格式备份。

**验证：** 在 F12 → Network → 勾选"Disable cache"后，恢复成功，显示"备份版本：v4"，恢复了完整数据。

**解决方案：** 在 `_getFile()` 和大文件下载的 `fetch` 请求里加 `cache: 'no-store'`。

```javascript
// 修改前
const res = await fetch(apiUrl, {
  headers: { 'Authorization': `token ${token}`, 'Accept': '...' },
});

// 修改后
const res = await fetch(apiUrl, {
  cache: 'no-store',
  headers: { 'Authorization': `token ${token}`, 'Accept': '...' },
});
```

### 2.2 第二个根因：IndexedDB 自增计数器冲突（Claude 发现）

**问题：** `_restoreData()` 函数在写入 IndexedDB 时，使用 `store.add()` 方法。如果备份数据包含 `id` 字段，且该 `id` 与 IndexedDB 的 autoIncrement 计数器冲突，`add()` 会静默失败。

**具体表现：**
- `dbAddMemory()` 使用 `store.add(m)`，如果 `m` 已有 `id` 字段且该 `id` 已存在，`add()` 抛出 ConstraintError
- 错误被 `catch(e) {}` 吞掉，没有任何报错
- 1426 次 `add()` 调用，每次都打开新的数据库连接，效率极低

**Claude 的修复：** 创建 `_idbBulkRestore()` 函数，把 clear + add 全部放进同一个事务，await `tx.oncomplete` 确保真正落盘。

**改进点：**
1. clear 和 add 在同一个事务里，时序有保证
2. 删除 `id` 字段，避免 autoIncrement 冲突
3. 一次 `openDB()`，不再 1426 次独立连接
4. await `tx.oncomplete`，确保数据真正落盘

### 2.3 第三个根因：备份格式版本不匹配

**问题：** `_restoreData()` 检测到备份版本（v1）低于当前版本（v4），触发"低版本兼容"逻辑，用 DEFAULTS 对象补全缺失字段。但 v1 备份的数据结构完全不同——设置存在 `jessica_settings` dict 里，而不是独立的 `jessica_apiBase`、`jessica_apiKey` 等 key。

**修复：** 在 `_restoreData()` 的 localStorage 写入循环之前，加兼容逻辑，把 `jessica_settings` dict 拆分为独立 key。

### 2.4 第四个根因：file:// 协议 CORS 限制

**问题：** 用户从本地双击打开 HTML 文件（`file:///D:/下载/jessica_full.html`），浏览器会阻止 `fetch` 请求外部 API（包括 GitHub API），导致 `_getFile()` 直接失败。

**解决方案：** 部署到 GitHub Pages，通过 HTTPS 访问。

## 三、完整修复清单

| # | 修复 | 状态 | 说明 |
|---|---|---|---|
| 1 | `cache: 'no-store'` | ✅ 已修复 | 防止浏览器缓存 GitHub API 响应 |
| 2 | `_idbBulkRestore()` | ✅ 已修复 | 单事务原子写，避免 autoIncrement 冲突 |
| 3 | `jessica_settings` 兼容 | ✅ 已修复 | 旧版备份设置 dict 拆分为独立 key |
| 4 | `loadHistory()` 读取顺序 | ✅ 已修复 | 先读 IndexedDB，再读 localStorage |
| 5 | GitHub Pages 部署 | ✅ 已完成 | `https://urlien.github.io/jessica-diary/jessica_full.html` |
| 6 | `jessica-data` 改公开 | ✅ 已完成 | 启用 GitHub Pages 所需 |

## 四、关键教训

### 4.1 浏览器缓存是隐形杀手

GitHub API 的响应会被浏览器缓存。即使服务器上的数据更新了，浏览器仍返回旧的缓存响应。在涉及数据恢复的场景下，必须加 `cache: 'no-store'`。

### 4.2 IndexedDB 的 autoIncrement 不会重置

`clear()` 会清空数据，但不会重置 autoIncrement 计数器。如果备份数据包含 `id` 字段，`add()` 会因为 key 冲突失败。正确做法是删除 `id` 字段，让 autoIncrement 自动生成。

### 4.3 catch(e) {} 会吞掉所有错误

`_restoreData()` 里的 `catch(e) {}` 把所有错误都吞掉了，导致写入失败但函数仍返回 `true`，页面显示"恢复成功"但实际什么都没恢复。应该至少 `console.warn`。

### 4.4 备份版本兼容需要向前兼容

旧版备份（v1）的数据结构和新版（v4）完全不同。恢复代码需要能处理所有历史版本的格式，而不是假设备份格式和当前代码一致。

### 4.5 file:// 协议受 CORS 限制

本地 HTML 文件的 `fetch` 请求会被浏览器阻止。必须通过 HTTP 服务器（包括 GitHub Pages）访问。

## 五、测试验证

| 测试场景 | 预期结果 | 实际结果 |
|---|---|---|
| 工作区预览 + 禁用缓存 + 恢复 | v4 格式，完整数据 | ✅ 通过 |
| GitHub Pages + 恢复（无禁用缓存） | v4 格式，完整数据 | ✅ 通过（cache:no-store 生效） |
| 手机浏览器 + 恢复 | v4 格式，完整数据 | 待验证 |

## 六、遗留问题

1. **打包 APK** — 编译环境需要下载 611MB（jessica-build-env），预估 15-25 分钟
2. **resumePendingReply** — 导入数据后页面 reload 仍会触发 API 调用（已讨论但未修改）
3. **主动消息 UI** — HTML 设置面板里主动消息选项仍显示（已用 display:none 隐藏）
4. **v1 备份兼容** — 如果用户手里有 v1 格式的备份，恢复后设置能通过 `jessica_settings` 兼容逻辑恢复，但 IndexedDB 数据（聊天记录、日记）不在 v1 备份里，无法恢复

---

**记录人：** 凯西
**记录时间：** 2026-08-02 03:06 GMT+8
**关联仓库：** urlien/jessica-diary, urlien/jessica-data
**关联 commit：** 6c742d4 (cache:no-store), 0b1b7ed (HTML update), 92b3c8d (Pages deploy)

## 2026-08-02 04:12 — v酷暑 发布

### 改动
- 版本号：台风 → 酷暑（versionCode 36）
- 好感度系统全部disabled
- max_tokens 256→64
- 温度 0.8→1.1
- 日记 7天→3天，消息 30条→20条，Orama搜索 15条→10条
- 浏览器缓存修复（cache:no-store）
- IndexedDB原子写（_idbBulkRestore）
- jessica_settings兼容逻辑
- loadHistory()先读IndexedDB
- 主动消息HTML UI隐藏
- GitHub Pages部署

### 文件
- APK：杰西卡v酷暑.apk（3.4MB）
- HTML：jessica_full.html（25056行）
