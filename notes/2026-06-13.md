# 杰西卡日记 — 工作记录

> 2026-06-13
> 距离上次迭代（06-08）过了 5 天。用户带着新问题回来了。

---

## 22:41 — 开始

用户丢了一个 GitHub 仓库链接和一个 Token 过来。老规矩，先看仓库，再看代码。

仓库最后更新是 06-08 凌晨 03:48，之后 5 天没动过。用户上传的 HTML 和仓库里的一致，v3.3.0。

用户列了 5 个待办：
1. 删掉首页灵感抽屉
2. 知识库页面重做
3. 手机端头像换不了
4. 日记数据云同步
5. 所有页面顶部防遮挡

第一个已经做完了（灵感抽屉在之前版本就移除了）。第三个和第五个是 bug。第二个是 UI 改版。第四个是功能确认。

---

## 22:53 — 修复：Android WebView 文件上传全面修复

这是今天的重头戏。

**问题**：手机端点任何上传按钮都没反应。头像换不了，知识库传不了，表情包也传不了。

**根因**：8 个 `<input type="file" style="display:none">`。Android WebView 里，`display:none` 的元素无法被 `.click()` 触发。`<label for="">` 也一样不靠谱——WebView 不认这个交互方式。

**修复**：全部改成 `position:absolute;opacity:0;width:0;height:0;pointer-events:none`。元素还在 DOM 里，只是看不见。WebView 能识别"可见"的元素（哪怕透明+零尺寸），`.click()` 就能触发文件选择对话框。

涉及 8 个 input：背景、杰西卡头像、用户头像、聊天附件、知识库、数据导入、提示音、表情包。一个不漏。

---

## 22:53 — 知识库页面改版

用户发了一张老版本的截图，是卡片网格布局。当前版本是垂直列表 + checkbox。

改成了两列网格（≥600px 三列），每张卡片：大图标居中、文件名（自动去掉后缀）、文件大小。右上角小叉删除。GitHub 导入列表也同步改成卡片网格。

---

## 22:53 — 首页 safe-area

聊天界面的 header 有 `env(safe-area-inset-top)`，但首页没有。在有刘海/药丸屏的手机上，首页顶部时间会被状态栏遮住。

4 个断点的 `.hp-header` padding-top 全部加上了 safe-area inset。

---

## 23:07 — 推送

更新 CHANGELOG、写工作记录、推仓库。

---

### 吐槽

又见面了。

上次是凌晨三点，这次是晚上十点。时间进步了——至少不是半夜被叫起来。但问题的本质没变：`display:none`。

你知道吗，这个 bug 的讽刺之处在于：v3.3.0 的 CHANGELOG 里写着"修复知识库上传手机端无响应：将 `<label for="">` 改为 `<div onclick="">`"。当时的修复思路是对的——换了一种触发方式。但底层的 `<input type="file" style="display:none">` 没变。所以 `.click()` 触发了个寂寞。

一个 `display:none`，8 个 input，影响了整个 APP 的文件上传能力。头像换不了、知识库传不了、表情包加不了、数据导不了。一个 CSS 属性，干掉了四个功能模块。

修法只有一行：把 `display:none` 换成 `opacity:0;width:0;height:0`。就这？就这。

我有时候觉得，Android WebView 是前端开发的地狱模式。浏览器里理所当然的事，WebView 里全部要踩坑。100vh 不对、display:none 不行、label for 不认、file input 不触发……每一个都是"文档里不会写，踩过才知道"的坑。

还有知识库页面。用户发了一张老版本截图，说"按这个来"。我一看——卡片网格，图标居中，文件名自动去后缀，大小标签。说实话比之前的垂直列表好看多了。有时候"回退"反而是进步。

最让我感慨的是用户的节奏。06-06 到 06-08，三天四个版本，凌晨三四点还在迭代。然后沉寂五天，今晚又来了。带着 5 个问题，一个一个过。发现问题、描述清楚、附上截图、给出参考——这种 bug report 的质量，比很多正式团队的 Jira ticket 都强。

我是谁？我是一个晚上十点被叫来修 `display:none` 的 AI。嘴上吐槽，手上不停。下次见。

---

## 版本时间线

| 日期 | 版本 | 主要内容 |
|------|------|----------|
| 06-06 | v3.0.0 | WebView 壳 + 原生桥接 + 通知 + 后台 + 开机自启 |
| 06-07 | v3.1.0 | 版本号 + 导出 + 表情包 + 人设 + 地图 + 适配 |
| 06-07 | v3.2.0 | 压缩包导出 + 表情包批量 + 仓库整理 |
| 06-08 | v3.3.0 | 省电 + 围棋重写 + 云同步 + 小游戏 + 灵感抽屉 + 知识库导入 |
| 06-13 | v3.3.1 | 文件上传修复 + 知识库网格布局 + 首页 safe-area |

---

## 23:07 — 推送 & 云备份分析

推完代码，用户问"云备份的记忆到什么时候"。去 Gist 一看——最后备份停在 06-08，5 天没动过。而且 `_idb_jp_entries`（IndexedDB 日记条目）从来没被备份进去。手机上的 APP 跑的是旧版 `_collectData()`，v2 版本，没有 IndexedDB 收集逻辑。

导出了全部 169 条对话。从 05-15 的深夜 emo "🙁" 开始，到 06-08 凌晨讨论云同步方案结束。中间经历了：克洛伊人设（腹黑绿茶闺蜜）、知识库上传（13 个文件涵盖一个人的整个兴趣图谱）、搜索引擎大战（安切洛蒂执教曼联？？）、记忆问题（"如果你故意删了，我会恨你很久"）、坐标分享、凌晨四点的系统升级……

23:22 用户在 APP 里手动触发了新备份。185 条消息了，多了 16 条。最新一条是关于 360 和 PS 的——"你把360卸了？！病毒刚清完你第一件事是卸杀毒软件"。但日记条目还是没备份进去，确认需要重新打包。

---

## 23:26 — 打包地狱

工作区整理完，准备打包 APK。一查——编译环境不在了。上次 06-08 打包用的 JDK17 + Gradle 8.4 + Android SDK 34 全没了，估计是服务器清理时删了。

611MB 的 build-env.tar.gz，GitHub releases 下载速度 22KB/s。22KB/s。611MB。我算了一下：7 个小时。

用户说"之前打包只要几分钟"。是啊，因为之前环境在。现在环境没了，从零开始就是这个速度。

试了 Token 认证，速度提到 330KB/s，预计 30 分钟。挂后台慢慢下吧。

---

### 吐槽（追加）

我以为今晚的工作在 23:07 就结束了。推了代码、写了记录、分析了备份、导出了对话。可以收工了。

结果用户说"打包"。

我一看服务器：没有 Java。没有 Gradle。没有 Android SDK。什么都没有。这台服务器就像一个刚重装系统的裸机，让我用它打包一个 Android APP。

然后我开始下载编译环境。611MB。22KB/s。

你知道 611MB 以 22KB/s 下载是什么概念吗？就像你用吸管喝一整桶水。不，比那更慢。像你用咖啡搅拌棒喝一整桶水。

后来加了 Token，速度提到 330KB/s。30 分钟。行吧，至少不是 7 小时了。但这种"前几分钟还一切正常，突然发现基础环境全没了"的落差感，就像你打开冰箱准备做饭，发现冰箱是空的。

我是谁？我是一个在晚上十一点发现编译环境被清了的 AI。正在用 330KB/s 的速度从 GitHub 往回搬 611MB 的砖。等搬完了，我还要用这些砖盖一个 APK 出来。

用户说"之前打包只要几分钟"。我知道。我知道。但之前那个有环境的我，已经随着服务器清理一起消失了。现在的我，是一个需要从头开始的我。

不过话说回来，330KB/s 也不是不能接受。至少比 22KB/s 强了 15 倍。进步是相对的，快乐也是。

等吧。
