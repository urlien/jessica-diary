

## 2026-07-22（凌晨02:00）

### 背景
杰西卡 v3.4.0 "大暑" 版本的云同步系统出问题：备份只导出 localStorage 不导出 IndexedDB，导致7/4后的聊天记录丢失。

### 完成的工作

1. **备份系统重做：Gist → GitHub 私有 repo**
   - 新建 `urlien/jessica-data` 私有仓库，专门存杰西卡备份
   - 云同步底层从 GitHub Gist API 改为 GitHub Contents API
   - 用户操作不变：粘 token → 点备份/恢复
   - 解决 Gist 10MB 上限问题

2. **完整备份 JSON 组装**
   - 聊天记录 763 条（6/8 ~ 7/20）解析自 `杰西卡完整记录.txt`
   - 日记 48 篇（缺1篇，后续补）
   - 全部设置信息写入
   - 头像去掉（1MB base64 → 默认"J"）
   - 修复 `loadHistory()` 优先读 localStorage 导致恢复后仍显示旧数据的 bug

3. **API 切换**
   - MiMo → DeepSeek（`https://api.deepseek.com`，`deepseek-v4-pro`）

4. **人设更新**
   - 杰西卡人设 + 用户档案同步更新

### 当前状态

| 项目 | 状态 |
|------|------|
| 云同步 | ✅ 改为 GitHub repo，已测试可用 |
| 聊天记录 | ✅ 763 条已导入 |
| 日记 | ⚠️ 48/49 篇（缺1篇，图片识别未跑通） |
| 设置 | ✅ DeepSeek API + 人设 + 用户档案 |
| 头像 | ✅ 去掉，用默认"J" |
| APK | ❌ 未重新编译 |

### 待办（巨量）
- [ ] 补缺失的第49篇日记
- [ ] 重新编译 APK（杰西卡v3.4.1）
- [ ] 有声书 TTS 生成
- [ ] 有声书 App 开发
- [ ] 杰西卡 emotion-engine 测试
- [ ] 云同步改为 GitHub repo 的代码同步到所有设备

### 技术笔记
- `loadHistory()` 优先读 `localStorage.jessica_messages`，不读 IndexedDB 的 `_idb_memories`。恢复备份时必须同时写入这两个地方
- GitHub token `ghp_O3…siU0` 在 Python requests 中会触发 latin-1 编码错误（Unicode 省略号），curl 和 configparser 没问题
- `_restoreData()` 不会清空 localStorage，旧 key 不会被覆盖——必须在备份 JSON 里包含所有需要恢复的 key
