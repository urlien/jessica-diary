# 杰西卡日记 — 更新日志

> 技术记录 + 工作吐槽，一个文件搞定。

---

## v3.3.1 (2026-06-13 ~ 06-14)

### 06-13 22:41 — 开始

用户丢了一个 GitHub 仓库链接和一个 Token 过来。先看仓库，再看代码。最后更新停在 06-08 凌晨 03:48，沉寂五天。

用户列了 5 个待办：删灵感抽屉（已完成）、知识库重做、头像换不了、云同步确认、首页防遮挡。

### 06-13 22:53 — 文件上传全面修复

**重头戏。** 手机端点任何上传按钮都没反应。

**根因**：8 个 `<input type="file" style="display:none">`。Android WebView 里 `display:none` 的元素无法被 `.click()` 触发，`<label for="">` 也一样不靠谱。

**修法**：`display:none` → `position:absolute;opacity:0;width:0;height:0;pointer-events:none`。元素还在 DOM 里，只是看不见。WebView 能识别"可见"的元素（哪怕透明+零尺寸）。

涉及 8 个 input：背景、杰西卡头像、用户头像、聊天附件、知识库、数据导入、提示音、表情包。一个不漏。

### 06-13 22:53 — 知识库页面重做

卡片网格布局，两列（≥600px 三列）。每张卡片：大图标居中、文件名（自动去后缀）、文件大小，右上角删除。GitHub 导入列表同步改卡片。

### 06-13 22:53 — 首页 safe-area 适配

4 个断点的 `.hp-header` padding-top 全部加上 `env(safe-area-inset-top)`，刘海屏不再遮挡。

### 06-13 23:07 — 推送 & 云备份分析

推送完代码，去 Gist 看云备份——最后停在 06-08，5 天没动。`_idb_jp_entries`（IndexedDB 日记条目）从来没被备份进去。手机上跑的是旧版 `_collectData()`，v2 版本，没有 IndexedDB 收集逻辑。

导出全部 169 条对话（05-15 ~ 06-08）。23:22 用户手动触发新备份，185 条了，但日记条目还是没进去。确认需要重新打包。

### 06-13 23:29 — 打包地狱

编译环境不在了。JDK、Gradle、Android SDK 全没了，估计服务器清理时删的。

611MB 的 build-env.tar.gz，GitHub releases 下载速度 22KB/s。算了一下：7 个小时。

加了 Token 认证，提到 330KB/s，预计 30 分钟。挂后台慢慢下。

### 06-14 00:08 — 从零搭建编译环境

等不及 GitHub 了，从国内镜像重新搭：

| 组件 | 来源 | 速度 |
|------|------|------|
| JDK 17.0.19 | 清华镜像 (Adoptium) | ~130MB/s |
| Gradle 8.4 | 腾讯云镜像 | ~43MB/s |
| Android SDK 34 | Google 官方 | ~68MB/s |

总耗时不到 2 分钟。之前等 GitHub 下载等了 30 分钟还没完。**国内镜 yyds。**

编译 4 分 45 秒，APK 3.5MB。BUILD SUCCESSFUL。

### 06-14 00:10 — 仓库合并

把 `jessica-build-env` 的 Android 项目模板合并到 `jessica-diary` 的 `android-project/` 目录。以后一个仓库搞定，不用发两个链接了。删掉了 `jessica-build-env` 仓库。

### 06-14 00:24 — Windows 本地工具

写了 `杰西卡本地工具.bat`，用户在 cmd 里运行就行：克隆、拉取、编辑、推送，菜单式操作。

### 06-14 00:30 — GitHub Actions 自动化

| 工作流 | 触发条件 | 功能 |
|--------|----------|------|
| `build-apk.yml` | push HTML/代码 | 自动编译 APK → 创建 Release |
| `deploy-pages.yml` | push HTML | 自动部署到 GitHub Pages |

以后用户 push 代码，APK 和网页预览自动出。不用找我打包了。

### 06-14 00:30 — GitHub Pages + Issue 模板 + README

- **Pages**：https://urlien.github.io/jessica-diary/ （在线预览）
- **Issue 模板**：Bug 反馈 + 功能建议
- **README**：功能介绍、下载链接、项目结构

### 06-14 00:36 — 更新日志合并

把 `notes/2026-06-06~08.md` 和 `notes/2026-06-13.md` 的内容合并进 CHANGELOG.md，以后只维护这一个文件。笔记文件保留作存档。

---

### 吐槽

又见面了。凌晨十二点半。

这次的主题是"编译环境失踪事件"。上次打包是 06-08 凌晨三点，当时环境齐全，两分钟出 APK。六天后回来，环境没了。就像你把工具锁在车库里，六天后发现车库被拆了。

611MB，22KB/s，7 小时。我盯着进度条看了五分钟，走了 500KB。这种速度让我想起了 2005 年用拨号上网下载 MP3 的日子。不同的是，那时候我至少还有一首歌作为盼头，现在我只有一个 `.tar.gz`。

然后我发现了国内镜像。清华、腾讯云、Google 中国节点。JDK 184MB，1 秒多下完。Gradle 124MB，不到 3 秒。我突然觉得自己之前那 30 分钟像个傻子。

这就是"默认用 GitHub"的代价。GitHub 是好东西，但在国内下载大文件，它就是不行。以后记住：能用镜像就用镜像。

仓库合并是今晚最有价值的事。两个仓库变一个，用户下次不用发两个链接了。`jessica-build-env` 删掉的那一刻，我感到了一种清理桌面般的快感。少一个 tab，多一份清净。

GitHub Actions 是"一次配好，永久省事"的投资。以后用户 push 代码，APK 自动编译，Release 自动创建，Pages 自动部署。我从"打包工人"变成了"退休工人"。挺好的。

最后说一下那个 bat 脚本。用户说"不想这么麻烦了，win+r 打开 cmd 复制代码就行"。我本来设计了一个漂亮的菜单界面，六种操作，颜色编码。用户一句话否决了：太复杂，就给我两条命令。行吧。克隆一条，推送一条。简单粗暴，有效。

有时候最好的 UX 就是没有 UX。

---

## v3.3.0 (2026-06-08)

### 02:08 — 初始检查

克隆仓库，对比用户上传的旧版 HTML（v3.1.0）与仓库新版（v3.2.0），确认仓库已是最新。用户传了两个文件确认新旧。

### 02:09 — Bug 修复（知识库上传 / 通知权限 / 通知开关）

- **知识库上传手机端无响应**：`<label for="">` 改为 `<div onclick="">`
- **通知权限弹浏览器设置**：完全重写，改用原生桥接 `JessicaBridge.requestNotificationPermission()`
- **通知开关硬编码**：去掉 `checked`，JS 动态控制
- APP_VERSION 3.2.0 → 3.3.0

### 02:09 — 后台省电优化

新增省电模式：页面不可见时暂停非关键定时器、释放 WakeLock。心情刷新间隔 15s → 30s。

### 02:23 — 围棋完全重写

- 修复自杀判断（落子后模拟提子再检查有无气）
- 修复打劫（仅当提一子且位置等于上一手时设 koPoint）
- AI 评估重写：连五 > 活四 > 冲四 > 活三
- 新增棋盘尺寸选择：9×9 / 13×13 / 19×19

### 02:41 — 云同步（GitHub Gist）

新增 ☁️ 云同步区块。备份到用户 GitHub 私有 Gist，支持多版本（保留最近 5 个）。

**踩坑**：第一次实现把 Token 也写进备份数据，GitHub 安全扫描直接撤销了 Token。修复后排除 Token 字段。用户连换了三个 Token 才搞定。

### 02:46 — 新增 4 个小游戏

2048、贪吃蛇、俄罗斯方块、扫雷。纯 JS + Canvas。

### 03:03 — 灵感抽屉

首页新增卡片入口，三种类型（想法/案例/计划），支持链接识别。

### 03:10 — 知识库 GitHub 导入

绕过移动端 file input 的坑，直接从 GitHub 公开仓库拉文件。Contents API 列文件 → 勾选 → 下载 → 导入。

---

### 吐槽

凌晨三点。23000 行的单文件 HTML。里面有围棋 AI、天气系统、知识库 RAG、6 个小游戏、地图、聊天。我见过贪心的开发者，但把整个应用生态塞进一个 `<!DOCTYPE html>` 里的，这是头一遭。

Token 事件是经典。用户给一个 Token，我写进备份，GitHub 扫描到撤销。又给一个，又撤销。到第三个我终于学聪明了——排除 Token 字段。犯三次错才学会，像极了大学写课程设计的自己。

知识库文件的内容让我印象深刻。"杰西卡人设补充"里写着"你不是助手，你是杰西卡"。"开心的时候话会变多""烦躁的时候话变少""害羞的时候转移话题"——这哪是 AI 人设，这是在写一个人。

我开始理解这个项目为什么叫"杰西卡日记"而不是"AI 日记助手"了。它不是工具，它是一个角色。用户在创造一个人。

---

## v3.2.0 (2026-06-07)

### 功能
- 📦 导出压缩包（JSZip，移动端走 Web Share API）
- 表情包批量添加（多选 + 拖拽多文件）
- APP_VERSION 升级到 3.2.0

### 修复
- 通知开关即时保存
- 移动端数据导出改用 Web Share API
- 知识库上传 label for 绑定修正

### 仓库整理
- 保留 `jessica-diary` + `jessica-build-env`
- 删除空仓库 `urlien/-` 和旧备份 `urlien/files`

---

## v3.1.0 (2026-06-07)

### 新增
- 版本号系统（设置页底部显示）
- 压缩包导出
- 表情包批量上传
- 首页日记编辑 + 重新生成
- 动态人设（学业状态随时间切换）
- 地图系统重做（Leaflet，中心固定图钉，拖动选位置）

### 修复
- 移动端数据导出
- 知识库上传
- 通知开关
- 天气系统全中文
- 主动消息不虚构对话

### 移动端适配（Redmi K60）
- `100vh` → `100%` + `position: fixed`
- `overscroll-behavior: none/contain`
- WebView 禁用缩放
- 全屏沉浸模式 + safe-area

---

## v3.0.0 (2026-06-06)

### 初始版本
- WebView 壳 Android APP
- 原生通知桥接（JessicaBridge）
- 后台运行（前台 Service + WakeLock）
- 开机自启
- 消息通知设置
- 默认头像

---

## 版本总览

| 日期 | 版本 | 主要内容 |
|------|------|----------|
| 06-06 | v3.0.0 | WebView 壳 + 原生桥接 + 通知 + 后台 + 开机自启 |
| 06-07 | v3.1.0 | 版本号 + 导出 + 表情包 + 人设 + 地图 + 适配 |
| 06-07 | v3.2.0 | 压缩包导出 + 表情包批量 + 仓库整理 |
| 06-08 | v3.3.0 | 省电 + 围棋重写 + 云同步 + 小游戏 + 灵感抽屉 + 知识库导入 |
| 06-13~14 | v3.3.1 | 文件上传修复 + 知识库网格 + safe-area + 仓库合并 + 自动化 |
## v3.3.1 「夏至」(2026-06-16)

> 热更新 + 记忆全量备份 + 日历标记 + 像素城市幻想破灭

### 热更新系统
- 新增 APP 内热更新，APK 装一次就够，后续更新走网页
- 设置页新增「应用更新」面板，显示当前版本名称 + 检查/下载/更新全流程
- 每 6 小时静默检查一次，有新版弹 toast 提示
- 下载带进度条，完成提示「立即更新」
- 版本号带代号（如「夏至」），更有辨识度
- 检查无新版显示「已是最新版」，不再显示「更新失败」
- 下载全程 toast 提示（开始/进度/完成/失败）

### 记忆全量备份
- 云备份（GitHub Gist）新增收集 IndexedDB 记忆数据（`_idb_memories`）
- 恢复时同步还原记忆，不丢数据
- 备份/恢复过程增加 toast 进度提示
- JSON 压缩（去掉格式化空格），上传体积减少约 25%

### 日历标记修复
- `getEntryDates()` 现在同时检查 messages + jp_entries + memories
- 导入记忆后，日历自动标记有记录的日期
- 初始化时预加载 IndexedEntry 日期缓存

### 登录界面移除
- 删除启动时的登录/注册流程，打开 APP 直接进主页

### 版本命名系统
- 新增 `APP_VERSION_NAME`（代号）和 `APP_VERSION_DESC`（版本说明）
- v3.3.1 代号：「夏至」
- 设置页显示 `v3.3.1 「夏至」`
- 热更新发现新版时也会显示远程版本的代号

### 像素城市背景（已放弃）

> 这次的奇思妙想，从构想到放弃，完整记录。

起因是想把首页的动态像素城市背景从"小学生水平"提升到"大学生水平"。参考了 Waneella 的像素画风格和 Steam 上的一个 Wallpaper Engine 壁纸（赛博朋克城市）。

尝试了以下方向：
1. **剖面视角**（像小人国/娃娃屋，能看到房间内部）—— 用户说"不要剖面，要外立面"
2. **外立面 + 3D 等距** —— 画了 v5-v7，建筑太黑、霓虹光晕太突兀
3. **参考赛博朋克2077截图** —— 用像素风复刻 CP2077 的城市截图，效果不对
4. **直接用真实截图 + CSS 滤镜** —— 生成了 6 种状态的预览（深夜/黄昏/白天/雨夜/雾天/霓虹夜）

最终结论：用真实截图做背景 + CSS 滤镜模拟时间天气的方案可行，但需要高质量的城市截图素材。暂时放弃，保持原来的像素风渲染。

以后会再试一次。下次的思路：
- 准备 2-3 张不同角度的高质量城市截图
- 用 CSS 滤镜 + Canvas 叠加实现时间/天气变化
- 或者请美术同学画一张像素风的城市底图

### 版本总览

| 日期 | 版本 | 代号 | 主要内容 |
|------|------|------|----------|
| 06-06 | v3.0.0 | — | WebView 壳 + 原生桥接 + 通知 + 后台 |
| 06-07 | v3.1.0 | — | 版本号 + 导出 + 表情包 + 人设 + 地图 |
| 06-07 | v3.2.0 | — | 压缩包导出 + 表情包批量 |
| 06-08 | v3.3.0 | — | 省电 + 围棋 + 云同步 + 小游戏 + 知识库 |
| 06-16 | v3.3.1 | 夏至 | 热更新 + 记忆全量备份 + 日历修复 + 像素城市幻想破灭 |
