

## 2026-07-22（凌晨02:00）

### 背景
杰西卡 v3.4.0 "大暑" 版本的云同步系统出问题：备份只导出 localStorage 不导出 IndexedDB，导致7/4后的聊天记录丢失。

### 完成的工作

1. **备份系统重做：Gist → GitHub 私有 repo**
   - 新建 `urlien/jessica-data` 私有仓库，专门存杰西卡备份
   - 云同步底层从 GitHub Gist API 改为 GitHub Contents API
   - 用户操作不变：粘 token → 点备份/恢复
   - 解决 Gist 10MB 上限问题

2. **完整备份 JSON 组装**
   - 聊天记录 763 条（6/8 ~ 7/20）解析自 `杰西卡完整记录.txt`
   - 日记 48 篇（缺1篇，后续补）
   - 全部设置信息写入
   - 头像去掉（1MB base64 → 默认"J"）
   - 修复 `loadHistory()` 优先读 localStorage 导致恢复后仍显示旧数据的 bug

3. **API 切换**
   - MiMo → DeepSeek（`https://api.deepseek.com`，`deepseek-v4-pro`）

4. **人设更新**
   - 杰西卡人设 + 用户档案同步更新

### 当前状态

| 项目 | 状态 |
|------|------|
| 云同步 | ✅ 改为 GitHub repo，已测试可用 |
| 聊天记录 | ✅ 763 条已导入 |
| 日记 | ⚠️ 48/49 篇（缺1篇，图片识别未跑通） |
| 设置 | ✅ DeepSeek API + 人设 + 用户档案 |
| 头像 | ✅ 去掉，用默认"J" |
| APK | ❌ 未重新编译 |

### 待办（巨量）
- [ ] 补缺失的第49篇日记
- [ ] 重新编译 APK（杰西卡v3.4.1）
- [ ] 有声书 TTS 生成
- [ ] 有声书 App 开发
- [ ] 杰西卡 emotion-engine 测试
- [ ] 云同步改为 GitHub repo 的代码同步到所有设备

### 技术笔记
- `loadHistory()` 优先读 `localStorage.jessica_messages`，不读 IndexedDB 的 `_idb_memories`。恢复备份时必须同时写入这两个地方
- GitHub token `ghp_O3…siU0` 在 Python requests 中会触发 latin-1 编码错误（Unicode 省略号），curl 和 configparser 没问题
- `_restoreData()` 不会清空 localStorage，旧 key 不会被覆盖——必须在备份 JSON 里包含所有需要恢复的 key

---

## 2026-07-31 发现的问题

### 1. 备份恢复失败：Unexpected end of JSON input

**现象：** 安装杰西卡v三伏APK后，导入数据时报错"恢复失败: Unexpected end of JSON input"

**原因：** 备份文件1.85MB，超过GitHub API的1MB限制。API返回`encoding: "none"`，`content`字段为空。`atob("")`解码出空字符串，`JSON.parse("")`报错。

**相关代码：** `cloudSync.restore()` 函数
```javascript
const content = decodeURIComponent(escape(atob(file.content)));
const data = JSON.parse(content);
```

**修复方案：** 当`file.content`为空时，改用`file.download_url`直接下载文件内容。

**状态：** ❌ 待修复

### 2. 两层状态栏系统（新功能，待测试）

**改动：** 状态栏从随机切换改为两层架构
- 第一层：日常节奏（基于时间表自动轮转，每8分钟切换）
- 第二层：对话同步（从杰西卡回复中提取活动关键词，实时更新）

**状态：** ❌ 未测试

### 3. Token计数显示（新功能）

**改动：** 聊天输入框下方新增token消耗显示
- 每次对话后自动更新
- 数据来源：DeepSeek API响应的`usage`字段
- stream_options加了`include_usage: true`

**状态：** ❌ 未测试

### 4. Output限制

**改动：** 主聊天`max_tokens`从8192改为1024

**状态：** ✅ 已生效

### 5. Input上下文优化

**改动：**
- `messages.slice(-30)` → `slice(-20)`
- 日记注入从全部改为最近5篇
- 知识库搜索从3个/500字改为2个/300字
- `buildMemoryContext`从7天改为5天

**状态：** ✅ 已生效

### 6. 禁止动作描写

**改动：** 在人设prompt的"失败模式纠正"中加了禁止（）动作描写的指令

**状态：** ❌ 未测试

### 7. 地图指针修复

**改动：** z-index从10提升到1000，新增绿色光点crosshair

**状态：** ❌ 未测试

### 8. 文件上传修复

**改动：** `#fileInput` CSS从`pointer-events: none`改为`1x1px`隐藏

**状态：** ❌ 未测试

### 待办更新
- [ ] 修复备份恢复bug（>1MB文件用download_url）
- [ ] 测试两层状态栏系统
- [ ] 测试Token计数显示
- [ ] 测试禁止动作描写
- [ ] 测试地图指针
- [ ] 测试文件上传
- [ ] 测试禁止动作描写
- [ ] 声音克隆测试
- [ ] 全部23章用克隆声音重新生成

---

## 2026-08-01 凯西记录

### 备份恢复21号问题（未解决）

**现象：** 从云端恢复备份后，聊天记录只显示到7/21。GitHub备份数据完整（1426条记忆，到7/30），但恢复后IndexedDB里的数据还是旧的。

**已尝试的修复（都没用）：**
1. loadHistory()改为先读IndexedDB再读localStorage
2. _restoreData里store.put()改为正确await（不再fire-and-forget）
3. 写入memories前delete id（避免autoIncrement冲突）

**可能的根因：** IndexedDB自增计数器与恢复数据冲突（Claude追踪到，但没给出具体修复方案）

**状态：** ❌ 未解决

### 主动消息UI

**现象：** 设置面板里主动消息选项仍然显示

**处理：** 用display:none隐藏了HTML UI。JS代码仍是disabled状态（return;在函数开头）

**状态：** ✅ 已隐藏（临时方案）

### tokens优化

- 好感度系统disabled（代码+备份）
- max_tokens 256→64
- 温度 0.8→1.1
- 日记 7天→3天
- 消息 30条→20条
- Orama搜索 15条→10条

**状态：** ✅ 已完成（未验证效果）
